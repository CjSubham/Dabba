#port connection
server.port = 8088
spring.datasource.name=payment
#connect to postgresql url,username and password 
#url
spring.datasource.url=jdbc:postgresql://localhost:5432/payment
#username
spring.datasource.username=postgres
#password
spring.datasource.password=subham
#Configure the JPA
spring.jpa.show-sql=true

#Hibernate properties
#The sql dialect makes hibernate to generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update
